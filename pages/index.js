// pages
import ReactComponents from './reactComponents';

// layout
import { DefaultLayout } from '../layout';

export default ({
}) => {
  return (
    <DefaultLayout>
      <ReactComponents />
    </DefaultLayout>
  );
}